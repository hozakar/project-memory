{
  "name": "project-memory-mcp-server",
  "version": "0.1.0",
  "description": "MCP companion server for project-memory skill — semantic search over phases and decisions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npx ts-node src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@lancedb/lancedb": "^0.6.0",
    "@xenova/transformers": "^2.17.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}